/**
the virtual keyword in base class gives the child class option that if they want they can override the method and if they don't want
they can choose not to implement They Don't force child class to override.

**/
